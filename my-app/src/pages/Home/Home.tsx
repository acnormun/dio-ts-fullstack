import { Box } from "@chakra-ui/react";
import { LoginCard } from "../../components/LoginCard/LoginCard";

export const Home = () => {
  return (
    <Box padding="25px">
      <LoginCard />
    </Box>
  );
};
